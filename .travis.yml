language: scala
jdk: openjdk8
git:
  depth: false
before_install:
  - mkdir .bin
  - curl -Lo .bin/sbt https://raw.githubusercontent.com/coursier/sbt-extras/36eba4ce871ea49e6ee1164e8694fc305a680880/sbt
  - chmod +x .bin/sbt
  - export PATH="$(pwd)/.bin:$PATH"
cache:
  directories:
  - $HOME/.cache
  - $HOME/.sbt
branches:
  only:
  - master
  - /^v\d+\.\d+.*$/ # tagged versions
stages:
  - name: test
  - name: release
    if: (branch = master AND type = push) OR (tag IS present)
jobs:
  include:
  - script: ./.travis.sh
  - stage: release
    script: sbt ci-release
